def f(s)
    var array arrof 3, x = s
    in
        array = 2:
        print(x):
        // same as array[0] = 2
        print(array)
        // same as print(array[0])
    end
;

def main()
    f(3)
;