def main()
    for i = 1,
        i <= 2
    in
        print(i * 10) :
        for j = 0,
            j <= 8,
            1
        in
            if !(j % 2) then print(-j) else print(j) fi
        end 
    end
;