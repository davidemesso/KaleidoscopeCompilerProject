def f(s)
    var array arrof 3, x = s
    in
        array = 20:
        array[1] = 50:
        print(array[1]):
        print(array[0])
        // same as print(array[0])
    end
;

def main()
    f(1)
;